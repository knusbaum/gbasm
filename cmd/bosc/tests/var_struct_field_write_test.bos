package main

import "string"

type point struct { x i64; y i64 }

fn main() {
	var p point = point{x: 1, y: 2}
	p.x = 99
	p.y = 88
	string.puti(p.x); string.putc('\n')
	string.puti(p.y); string.putc('\n')
}
