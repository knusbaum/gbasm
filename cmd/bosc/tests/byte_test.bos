package main

import "string"

fn main() {
	var b byte
	b = 'a'
	string.putb(b)
	string.puts("\n")
	// 97

	b = b + 1
	string.putb(b)
	string.puts("\n")
	// 98

	b = b + 200
	string.putb(b)
	string.puts("\n")
	// 42 (overflow)
}
