package main

import "string"

fn main() {
	var x i64
	x = 42

	var y i64
	y = 6

	var px *mut i64
	px = &x

	var py *mut i64
	py = &y

	// Volatile first operand.
	x = x / 7
	string.puti(*px)
	string.puts("\n")

	// Volatile second operand.
	y = 100 / y
	string.puti(*py)
	string.puts("\n")

	// Both volatile.
	x = x * y
	string.puti(*px)
	string.puts("\n")
}
