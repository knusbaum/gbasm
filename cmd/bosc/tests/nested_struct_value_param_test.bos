package main

import "string"

type inner struct { z i64 }
type outer struct { x i64; in inner }

fn modify(var o outer) {
	o.x = 999
	o.in.z = 888
}

fn main() {
	o outer := outer{x: 1, in: inner{z: 2}}
	modify(o)
	string.puti(o.x); string.putc('\n')
	string.puti(o.in.z); string.putc('\n')
}
