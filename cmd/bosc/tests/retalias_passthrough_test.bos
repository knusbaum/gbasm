package main

import "string"

// id returns its borrowed slice parameter unchanged. ReturnAliases=[[0]].
fn id(s byte[]) byte[] {
	return s
}

fn main() i64 {
	src byte[] := "passthrough"
	r byte[] := id(src)
	string.puts(r)
	string.puts("\n")
	return 0
}
