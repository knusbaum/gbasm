package main

import "string"

type foo struct {
	x i64
	y i64
}

fn main() {
	var f owned foo = foo{x: 1, y: 2}
	var fp *mut owned foo = &f
	fp.x = 10
	fp.y = 20
	string.puti(fp.x)
	string.puts("\n")
	string.puti(fp.y)
	string.puts("\n")
	dispose(fp)
}
