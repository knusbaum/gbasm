package main

import "string"

fn fact(n i64) i64 {
	if (n <= 1) {
		return 1
	}
	return n * fact(n - 1)
}

fn main() {
	string.puti(fact(0))
	string.puts("\n")
	string.puti(fact(1))
	string.puts("\n")
	string.puti(fact(5))
	string.puts("\n")
	string.puti(fact(10))
	string.puts("\n")
}
