package main

import "string"

type Foo struct {
	x  i64
	xp *?i64
}

fn bad() Foo {
	var s Foo := Foo{x: 10, xp: nil}
	s.xp = &s.x
	s = Foo{x: 20, xp: nil}
	return s
}

fn main() {
	var s Foo := bad()
	string.puti(s.x)
	string.puts("\n")
}
