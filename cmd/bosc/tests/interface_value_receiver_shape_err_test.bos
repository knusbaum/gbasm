package main

type my_err i64 {
	message(e *my_err) byte[] {
		return "bad"
	}
}

interface error_val {
	message(e self) byte[]
}

fn main(args byte[][]) {
	var e my_err = my_err(7)
	var v error_val = e
}
