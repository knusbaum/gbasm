package main
import "fmt"
// Bounds check fires on an out-of-range index into a global array.
var g i64[3] := [7, 8, 9]
fn main() {
	var i i64 := 0
	for (i = 0; i < 5; i = i + 1) { fmt.print("%d\n", g[i]) }
}
