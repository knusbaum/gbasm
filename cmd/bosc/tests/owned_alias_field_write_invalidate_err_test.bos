package main

struct leaf {
	val i64
}

struct box {
	val i64
	child owned *?owned mut leaf
}

fn destroy(b owned *owned mut box) {
	var alias *mut box = b
	var child owned *?owned mut leaf = b.child
	alias.child = nil
	free(b)
	if(child) {
		free(child)
	}
}

fn main() {}
