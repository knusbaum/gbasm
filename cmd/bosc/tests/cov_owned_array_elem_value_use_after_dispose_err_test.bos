package main

import "fmt"

// #8-class: a value copied out of an owned-array element, read after the
// array is disposed, must be rejected (the value aliases consumed storage).
fn main() {
	a owned i64[3] := [10, 20, 30]
	b i64 := a[1]
	dispose(a)
	fmt.print("%d\n", b)
}
