package main

// Borrowed slice parameter wrapped in a returned struct literal. The slice
// field carries the same lifetime constraint as a bare borrowed return —
// putting it inside the struct must not bypass the escape rule.

type B struct {
	buf byte[]
}

fn wrap(s byte[]) B {
	return B{buf: s}
}

fn main() {}
