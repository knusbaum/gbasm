package main

type leaf struct {
	val i64
}

type box struct {
	val i64
	child owned *?owned mut leaf
}

fn destroy(a owned *owned mut box) {
	var b *mut box := a
	var c *mut box := b
	var child owned *?owned mut leaf := a.child
	c.child = nil
	free(a)
	if(child) {
		free(child)
	}
}

fn main() {}
