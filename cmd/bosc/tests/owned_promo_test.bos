package main

import "string"

type Res struct {
	val i64
}

fn initialize(r *mut Res, v i64) {
	r.val = v
}

fn cleanup(r *owned Res) {
	string.puti(r.val)
	string.puts("\n")
	dispose(r)
}

fn main() {
	var r Res

	initialize(&r, 42)

	var res *owned Res := owned(&r)

	cleanup(res)
}
