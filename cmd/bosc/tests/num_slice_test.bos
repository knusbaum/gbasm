package main

import "string"

fn sumslice(ns i64[]) i64 {
	var i i64
	var s i64
	s = 0
	for (i = 0; i < string.lenn(ns); i = i + 1) {
		s = s + ns[i]
	}
	return s
}

fn main() {
	var arr i64[5]
	arr[0] = 10
	arr[1] = 20
	arr[2] = 30
	arr[3] = 40
	arr[4] = 50

	var ns i64[]
	ns = arr[:]
	string.puti(string.lenn(ns))
	string.puts("\n")
	string.puti(ns[2])
	string.puts("\n")
	string.puti(sumslice(ns))
	string.puts("\n")

	ns = arr[1:4]
	string.puti(string.lenn(ns))
	string.puts("\n")
	string.puti(sumslice(ns))
	string.puts("\n")
}
