package main

import "string"

fn main() {
	var a byte[10]
	a[0] = 1
	a[1] = 2
	a[2] = 3
	a[3] = 4
	var b byte[]
	b = a[:]
	string.putb(b[0])
	string.puts("\n")
	string.putb(b[1])
	string.puts("\n")

	b = a[:2]
	string.putb(b[0])
	string.puts("\n")

	b = a[2:]
	string.putb(b[0])
	string.puts("\n")

	var arr byte[50]
	slicetest(arr[:])

	var i i64
	var bb byte
	for(i = 0; i < 50; i = i + 1) {
		bb = arr[i]
	}

	var bs byte[]
	bs = arr[:]
	for(i = 0; i < 50; i = i + 1) {
		bb = bs[i]
	}

	// bs = arr[10:20]
	// for(i = 0; i < 50; i = i + 1) {
	// 	bb = bs[i]
	// }
	
	// boundstest()
}

// fn boundstest() {
// 	var arr byte[50]
// 	var bs byte[]
// 	bs = arr[:]
// 	var b byte
	
// 	b = bs[0]
// 	string.putb(b)
// 	string.puts("\n")
// 	b = bs[49]
// 	string.putb(b)
// 	string.puts("\n")
// 	// index out of bounds
// 	//b = bs[50]
// 	//string.putb(b)
// 	//string.puts("\n")
// }

fn slicetest(var bs mut byte[]) {
	var i i64
	i = 0
	i = i + 5

	bs[5] = 10
	bs[6] = 20

	string.puti(string.lenb(bs))
	string.puts("\n")
	
	bs = bs[i:]
	string.puti(string.lenb(bs))
	string.puts("\n")

	string.putb(bs[0])
	string.puts("\n")

	bs = bs[:i]
	string.puti(string.lenb(bs))
	string.puts("\n")

	i = 1
	bs = bs[i:i+1]
	string.puti(string.lenb(bs))
	string.puts("\n")
	
	string.putb(bs[0])
	string.puts("\n")
}
