package main

import "string.bo"

fn main() {
	var b byte
	b = 'h'

	putc(b)
	puts("\n")

	var s byte[]
	s = "Hello, World!"
	putc(s[0])
	puts("\n")

	var len i64
	len = lenb(s)

	var i i64
	for (i = 0; i < len; i = i + 1) {
		putc(s[i])
	}
	puts("\n")
}
