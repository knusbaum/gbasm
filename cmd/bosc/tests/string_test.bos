package main

import "string"

fn main() {
	var b byte
	b = 'h'

	string.putc(b)
	string.puts("\n")

	var s byte[]
	s = "Hello, World!"
	string.putc(s[0])
	string.puts("\n")

	var len i64
	len = len(s)

	var i i64
	for (i = 0; i < len; i = i + 1) {
		string.putc(s[i])
	}
	string.puts("\n")
}
