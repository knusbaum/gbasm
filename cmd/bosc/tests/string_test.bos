package main

import "string.bo"

fn main() {
	var b byte
	b = 'h'

	putc(b)
	puts("\n")

	var s str
	s = "Hello, World!"
	putc(s[0])
	puts("\n")

	var len num
	len = strlen(s)

	var i num
	for (i = 0; i < len; i = i + 1) {
		putc(s[i])
	}
	puts("\n")
	
}
