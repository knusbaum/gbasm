package main

import "string"

fn main() {
	var x i8
	x = 10

	// add/sub
	x = x + 5
	if (x == 15) { string.puts("add ok\n") } else { string.puts("add fail\n") }

	x = x - 7
	if (x == 8) { string.puts("sub ok\n") } else { string.puts("sub fail\n") }

	// signed: negative (neg = 0 - 1 = -1)
	var neg i8
	neg = 0
	neg = neg - 1
	if (neg < 0) { string.puts("neg lt ok\n") } else { string.puts("neg lt fail\n") }

	// signed overflow: 127 + 1 = -128 (negative)
	var mx i8
	mx = 127
	mx = mx + 1
	if (mx < 0) { string.puts("overflow ok\n") } else { string.puts("overflow fail\n") }
}
