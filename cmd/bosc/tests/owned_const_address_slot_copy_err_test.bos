package main

struct thing {
	val i64
}

fn main() {
	const a owned *owned mut thing = new(thing{val: 1})
	const slot **mut thing = &a
	const copy owned *mut owned thing = *slot
	free(a)
	free(copy)
}
