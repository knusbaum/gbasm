package main

import "string"

type FD i64
type Tag i16

fn openfd() owned FD {
	var fd FD
	fd = FD(3)
	return fd
}

fn closeit(fd owned FD) {
	string.puti(i64(fd))
	string.puts("\n")
	dispose(fd)
}

fn tagval(t Tag) {
	string.puti(i64(t))
	string.puts("\n")
}

fn main() {
	var fd owned FD
	fd = openfd()
	closeit(fd)

	var t Tag
	t = Tag(42)
	tagval(t)

	var n i16
	n = 100
	tagval(Tag(n))
}
