package main

import "string"

type wrap struct { p *mut i64 }

fn poke(var w wrap) {
	*w.p = 99
}

fn main() {
	var x i64 := 1
	var w wrap := wrap{p: &x}
	poke(w)
	string.puti(x); string.putc('\n')
}
