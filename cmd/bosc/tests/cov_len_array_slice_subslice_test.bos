package main

import "fmt"

// len: fixed array is a compile-time constant, slice is a runtime load, a
// subslice reports its own length.
fn main() {
	a i64[3] := [1, 2, 3]
	s i64[] := a[:]
	fmt.print("%d %d %d\n", len(a), len(s), len(a[1:3]))
}
