package main

// A binding initialized from a borrowed slice inherits the borrow.
// Returning the alias is the same escape as returning the parameter
// directly; an extra var indirection must not bypass the rule.

fn id(s byte[]) byte[] {
	var alias byte[] = s
	return alias
}

fn main() {}
