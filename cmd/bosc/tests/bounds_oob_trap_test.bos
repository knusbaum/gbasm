package main

import "fmt"

// A dynamic index past the end traps at runtime with a clear message
// (the bounds check). Exits non-zero; the .exit file records that.
fn main() {
	nums i64[3] := [10, 20, 30]
	var i i64 := 0
	for (i = 0; i < 10; i = i + 1) {
		fmt.print("%d\n", nums[i])
	}
}
