package main

import "string"

type dog i64 {
	speak(_ *dog) byte[] {
		return "woof"
	}
	name(_ *dog) byte[] {
		return "dog"
	}
}

type cat i64 {
	speak(_ *cat) byte[] {
		return "meow"
	}
	name(_ *cat) byte[] {
		return "cat"
	}
}

interface Animal {
	speak(a *self) byte[]
	name(a *self) byte[]
}

fn greet(a Animal) void {
	string.puts(a.name())
	string.puts(" says: ")
	string.puts(a.speak())
	string.puts("\n")
}

fn main() i64 {
	var d dog := 0
	var c cat := 0

	ad Animal := &d
	ac Animal := &c

	string.puts(ad.speak())
	string.puts("\n")
	string.puts(ac.speak())
	string.puts("\n")

	greet(ad)
	greet(ac)

	return 0
}
