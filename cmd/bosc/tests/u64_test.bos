package main

import "string"

fn main() {
	var x u64
	x = 100

	// add/sub
	x = x + 50
	if (x == 150) { string.puts("add ok\n") } else { string.puts("add fail\n") }

	x = x - 75
	if (x == 75) { string.puts("sub ok\n") } else { string.puts("sub fail\n") }

	// mul/div
	x = x * 4
	if (x == 300) { string.puts("mul ok\n") } else { string.puts("mul fail\n") }

	x = x / 3
	if (x == 100) { string.puts("div ok\n") } else { string.puts("div fail\n") }

	// unsigned: large value compares greater
	var big u64
	big = 10000000000
	if (big > 1000) { string.puts("big gt ok\n") } else { string.puts("big gt fail\n") }

	// unsigned underflow wraps: 0 - 1 = UINT64_MAX, which is > 0
	var z u64
	z = 0
	z = z - 1
	if (z > big) { string.puts("wrap ok\n") } else { string.puts("wrap fail\n") }
}
