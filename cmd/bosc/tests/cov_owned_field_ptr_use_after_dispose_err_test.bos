package main

import "fmt"

// A pointer borrow of an owned *value* field tracks via the struct origin;
// dispose invalidates it.
type box struct { x owned i64 }

fn main() {
	s owned box := box{x: 10}
	p *i64 := &s.x
	dispose(s)
	fmt.print("%d\n", *p)
}
