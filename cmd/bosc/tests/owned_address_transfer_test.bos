package main

import "string"

type foo i64 {
	hello(f *foo) {
		string.puts("hello\n")
	}
}

fn use_foo(f *owned foo) {
	f.hello()
	dispose(f)
}

fn main() i64 {
	var x owned foo = 0
	var y *owned foo = &x
	use_foo(y)
	return 0
}
