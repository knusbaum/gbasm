package main

import "string"

fn main() {
	// Call puts and puti via qualified name from package "string".
	// string.bo is mapped to "string" via the test runner's importcfg.
	string.puts("qualified call works\n")

	x i64 := 42
	string.puti(x)
	string.puts("\n")
}
