package main

// Same escape as `var alias byte[] = s`, but via an assignment after a
// separately-declared empty slice. This exercises the Assignment path
// of borrow-flag propagation, distinct from the VarDecl-init path.

fn id(s byte[]) byte[] {
	var alias byte[]
	alias = s
	return alias
}

fn main() {}
