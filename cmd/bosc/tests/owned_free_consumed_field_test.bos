package main

struct leaf {
	val i64
}

struct box {
	child owned *?owned mut leaf
}

fn destroy(b owned *owned mut box) {
	var child owned *?owned mut leaf = b.child
	free(b)
	if(child) {
		free(child)
	}
}

fn main() {
	const b owned *owned mut box = new(box{child: nil})
	destroy(b)
}
