package main

import "string"

fn main() {
	var x i64
	x = 0

	x = x - 1
	string.puti(x)
	string.puts("\n")

	x = x * 20
	string.puti(x)
	string.puts("\n")

	x = x / 2
	string.puti(x)
	string.puts("\n")
}
