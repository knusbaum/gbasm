package main

fn cond() bool {
	var b bool
	return b
}

fn main() {
	var p owned *?mut i64 := nil
	var i i64
	for(; i < 1; i = i + 1) {
		if (cond()) {
			p = alloc(i64)
			free(p)
			continue
		}
	}
}
