package main

import "string"

fn main(args byte[][]) {
	a i64 := 0xFF00
	b i64 := 0x0FF0
	string.puti(a & b)
	string.puts("\n")
	string.puti(a | b)
	string.puts("\n")
	flags i64 := 0x01 | 0x04
	string.puti(flags)
	string.puts("\n")
	mask i64 := flags & 0x04
	string.puti(mask)
	string.puts("\n")
}
