---
title: Hello, Boson
section: Basics
order: 1
---
Every Boson program starts with a `package` declaration and a `main`
function. Output goes through the `fmt` package, whose `print` function takes a
format string followed by any arguments — much like `printf` functions in other C-family languages.

Run the program to the right. Then try changing the message and running it again.
