---
title: Hello, Boson
section: Basics
order: 1
---
Every Boson program starts with a `package` declaration and a `main`
function. Output goes through the `fmt` package, whose `print` takes a
format string followed by any arguments — much like a familiar `printf`.

Run the program below. Then try changing the message and running it again.
