package main

import "fmt"

fn main() {
	flags i64 := 0x0F
	masked i64 := flags & 0x06
	fmt.print("masked = %d\n", masked)

	neg i64 := -1
	fmt.print("as signed:   %d\n", neg)
	fmt.print("as unsigned: %u\n", u64(neg))
}
