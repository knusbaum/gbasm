package main

import "fmt"

fn main() {
	greeting byte[] := "Boson"
	fmt.print("greeting = %s\n", &greeting)
	fmt.print("length   = %d\n", len(greeting))
	fmt.print("first byte = %c\n", greeting[0])
}
